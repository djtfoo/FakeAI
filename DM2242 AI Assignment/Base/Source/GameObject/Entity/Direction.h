#ifndef DIRECTION_H
#define DIRECTION_H

enum DIRECTION
{
    DIR_DOWN,   // the default direction; Entity's face visible on screen
    DIR_UP,
    DIR_RIGHT,
    DIR_LEFT,

    DIRECTIONS_TOTAL
};

#endif